-- 007 — отметка о том, что страницу объявления мы уже читали.
--
-- Ходим на страницы только за теми объявлениями, которые кому-то подошли, и
-- ровно один раз: 300 объявлений в час превратились бы в 300 лишних запросов
-- к доске.

ALTER TABLE listings ADD COLUMN details_at TEXT;
